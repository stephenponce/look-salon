<h1>Listing images</h1>

<table>
  <tr>
    <th> Images </th>
  </tr>

<% for image in @images %>
  <tr>
    <td> 
      <%= link_to (image_tag(image.public_filename(:thumb)), image.public_filename, :target=>:blank) %> <br />
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New image', new_image_path %>
