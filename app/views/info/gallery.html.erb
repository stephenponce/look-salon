<h2 class='heading'> Gallery </h2>
  <p>
    Lorem ipsum dolor sit amet, consectetur adipiscing elit. In ac felis sed ligula consectetur dapibus. 
  </p>
  <div class='boxed'>
    <% for image in @images %>
      <%= link_to_remote image_tag(image.public_filename(:thumb), :height=>'100', :class=>'gallery'), 	
		:url=> { :action=>'show_image_ajax', :id=>image },
		:onclick =>"new Effect.Fade('image_wrapper');",
		:complete=> "new Effect.Blinddown('image_wrapper', {duration:1.0})" %>
<!---		:update=> {:success=>'image_wrapper', :failure=>'err	ors' },
		:loading=> "$('image_wrapper').hide(); $('ajax_indicator').show();", 
		:complete=> "$('ajax_indicator').hide(); visual_effect(:toggle_appear)" %>
--->
    <% end %>
  </div>

